/**
 * Update Password Loading Skeleton
 * 
 * Simply re-exports the generic authentication loading skeleton 
 * to keep UI consistent and reduce code duplication.
 */
export { default } from '../login/loading';
